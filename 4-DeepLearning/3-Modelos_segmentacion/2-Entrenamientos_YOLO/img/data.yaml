train: images/train  # Carpeta de imágenes de entrenamiento
val: images/val      # Carpeta de imágenes de validación
test: images/test    # Ruta a las imágenes de prueba (opcional)

nc: 1  # Número de clases (ajusta según las clases en 'clases.txt')
names: ['floor'] 